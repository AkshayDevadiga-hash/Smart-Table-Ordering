PORT=3000
# Local dev: backend origin for the Node proxy (server.js).
API_URL=http://localhost:5000
# Cloudflare Pages build only: public backend URL (no trailing slash), e.g. https://your-app.up.railway.app
# PUBLIC_API_URL=https://your-app.up.railway.app
